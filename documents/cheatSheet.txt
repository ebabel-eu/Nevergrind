301-335-0726
/*
// findShapeIndex("#US", "#CA");
TweenMax.fromTo("#US", 5, { 
	morphSVG: '#US'
}, {
	morphSVG: {
		shape: "#CA",
		shapeIndex: 258
	}
}); 

*/


	$.ajax({
		type: 'GET',
		url: 'php/createGame.php',
		data: {
			name: name,
			players: players
		}
	}).done(function(data) {
		console.info("data: ", data);
	}).fail(function(e){
	}).always(function(){
	});

NEVERGRIND BUILD:
- minify & update into new incremented file name
- test new minified js using /#test to trigger test mode in dev
- upload using /minified folder into server root folder

<video autoplay src="http://nevergrind.com/videos/anderson_fags.webm" type="video/webm"></video>
MONETIZATION:
- Name change feature
- Account-wide bonuses for defeating nightmare/hell?
- Resurrection potions
- Built in bot?
- Daily use items? +XP scroll? +Speed scrolls?
- Upgrade items?
- Cooldown refresh?
- Gambling?
- Or sell stuff like global chat horns (lets you send global chat messages) etc.
- set an icon or change my leaderboard line border to gold or something is worth crystals.
- Setting up guilds etc can be monetized. (Guild stash space mostly)
- Bonus gold upon first purchase - (add $25 purchase)
- Small amount of gold for loggin in 3 days straight after registering
- gold bonus for the top 3 ranked players every day
- online for minutes/hours bonus 
- bonus every 3 days? Some kind of ranking bonus?
- more item space
	background:url('//d3t6yj0r8qins4.cloudfront.net/home.jpg');	
	
	
	select count(row) total, name, rarity from item where rarity>=3 and rarity<=4 group by name;


	
MAJOR BUGS:
- add payment via paypal/peanut
- add /r
- change my names to GM-?
- account whispering
- character graveyard + leaderboard
- more global announcements
- /friend list
- /squelch
- start inserting set/unique items into a table for later use
- need confirm email button on main page
- make mobs unlimited
- PAL: change skill that changes as you get mo' dead, + shield block valor talent
- SK: buff: engulfing darkness;
- SK: gasping frenzy +swings $("#mobIcons2").children().length
- ROG: assassinate talent
- php replace [a][img][iframe]
- CHAMPS: Great General, Scrappy, BLIGHTED
- remove Buff stats - replace with status checks
- buff thorns
- Create mailboxes 
-- add ability for mailboxes to restore Kickstarter items
- NG adjust skills/talents that affect run speed Rogue skill... cascading/engulfing darkness
- create DB view for class/talents
- add smith (existing upgrades), enchanter (add one stat to an item), and ??
- save password?
- sell all button?
- NG add opt out column for emails
- get rid of 
	character/item -> 
	status/timestamp/deleted/created/email columns
	quest email
- NG - enchant adds one rare property to an item
- get rid of JQuery UI
- buff magicians
- is it necessary to set src every time? Just display none it? or move outside the box
- load particle once.. change its color with easeljs

- Add professions:
- change upgrade item system to smithing system.
	- blacksmithing
	- leatherworking
	- Jewelcrafting
	- Tailoring
	- Enchanting
	- alchemy?
	- sockets?

<b>Notice</b>:  Undefined variable: characterEmail in <b>C:\xampp\htdocs\ng\php\loadData1.php</b> on line <b>201</b><br />


MINOR BUGS:
- pausing screws up DPS

IMPROVEMENTS:
- improve graveyard for HC characters

IDEAS:
- new champ traits: 
	- scrappy (1/4 size, takes double damage, 33% more physical damage) 
	- leader (summons add in 30 seconds)
- exceptional sets, elite sets

Forums forums
Signature:
[url=https://nevergrind.com/]Nevergrind - Free browser RPG[/url]
[url=https://nevergrind.com/forums]Forums[/url] | [url=https://nevergrind.com/leaderboards]Leaderboards[/url] | [url=https://nevergrind.com/nevergrounds]Nevergrounds[/url]

<a href="https://nevergrind.com/">Nevergrind - Free Browser RPG</a>
<a href="https://nevergrind.com/forums/">Forums</a> | <a href="https://nevergrind.com/leaderboards/">Leaderboards</a> | <a href="https://nevergrounds.com/nevergrounds/">Nevergrounds</a>

- Talents should be greyed out when you don't have the level req.
- Grey out (or red out) skills that can't be trained by your class.
- tooltip positioning logic
load particles later... show background right away.
ambush sound effect

- Make upgrades instant... ?
- 10 crystals per virgin boss kill?
- Remove upgrades.
- Mute on main page.
- Change all L20 for "main" skills.

7 31 10 12 16 24
========================================
$100:


		// make sure they own this character! check character ownership
		$query = "select email from characters where name=?";
		$stmt = $link->prepare($query);
		$stmt->bind_param('s', $name);
		$stmt->execute();
		$stmt->store_result();
		$stmt->bind_result($db_email);
		while($stmt->fetch()){
			$characterEmail = $db_email;
		}
		if($characterEmail!=$_SESSION['email']){
			exit;
		}


70*13  910
15*150 2250

giant link redirect to https; mode

115-125 and 170-180
Kickstarter tasks:
- TEST DATABASE BACKUPS
- create legendary items
- prevent talenting skills you don't have
1280x720?

- Receive-only mailbox for legendaries? Or just insert it...
- Trigger adding KS only items automatically to bank

KS update news:
- names will be unique!

Monetization ideas:


if(proc!==true){
		damEnh+=g.enhanceAllEquip;
		damEnh+=g['enhance'+mType[0].toUpperCase() + mType.slice(1)+'Equip'];
	}

- project 1999 / p1999 google search
kickstarter image searches

Possible update ideas:

  Spotlight on game mechanics
			Chain counter:
			1-5+ chain = 20 mf 20/9 = 2.22% chance unique
			6-10+ chain = 15 mf 35/9 = 3.88%
			11-15+ chain = 10 mf 45/9 = 5%
			16+ chain = 45 mf 90/9 = 10%
			
			150% exp bonus is possible from chain!
			
			Champ/boss/rare Bonus:
			+ magic find (diminishing returns) +2% +1.6% +1.2% +.8% +.4% (max 100 raw magic find)
			horde bonus adds: 10,  25,  50,  80% (max 80)
			
			Rare: +70 champBuff 
			champ: +25 champBuff
			boss = +200
			virgin boss = +700
			
			horde bonus is all divided by 10!
			
			-- chain combos,  horde bonus,  and magic find
			
-          More NG history/background
			- Progress Quest,  the untold story
-          Itemization in NG

-          Use of Magic Find in NG
===========================================

// crontab goodies
show cron jobs
crontab -l

open editor
crontab -e
// backup database
1 0 * * * mysql videos > /backups/ng_backup_$(date +%F).sql
0 0 * * * /usr/local/bin/mysqldump -uLOGIN -PPORT -hHOST -pPASS DBNAME | gzip -c > `date “+\%Y-\%m-\%d”`.gz
// restore database
mysql nevergrindDB < /backups/ng_backup_2015-03-26.sql

????
mysql nevergri_ngLocal > /home/users/web/b2535/moo.nevergrindcom2/public_html/backups/ng_backup_$(date +%F).sql
??

// animate to a circular path
function circX(cx,  r,  angle){
    return cx + r * (M.cos(angle * Math.PI/180));
}
function circY(cx,  r,  angle){
    return cx + r * (M.sin(angle * Math.PI/180));
}

var angle = M.random()*360;
var x = circX(500,  400,  angle);
var y = circY(200,  200,  angle);

		var x=(this.target);
		x.parentNode.removeChild(x);
				
		mysql_query("
			insert into chat (message,  cID,  name,  date,  time) 
				values ('".$msg."',  
				'".$cID."',  
				'".$name."',  
				CURRENT_DATE,  
				CURRENT_TIME);
		");	
		$result = mysql_query("
			select max(mID) from chat;
		");
			/*
		$string='';
		while($row=mysql_fetch_assoc($result)){
			$string.= $row['mID']."||";
			$string.= $row['message']."||";
			$string.= $row['name']."||";
		}
		*/
		$q2 = 'select game, count(row) c from `fwplayers` group by game';
		$result = $link->query($q2);
		
		$a = array();
		
		while($row = $result->fetch_assoc()){
			$a[$row['game']] = $row['c'];
		}
		
		insert into fwplayers (game, account, host) values (6, "abcdefg", 0);
		
		select w.name, count(p.game), p.account, w.max from fwGames w join fwplayers p on w.row=p.game group by p.game
		
		select w.row row, w.name name, count(p.game) players, w.max max, w.timer timer from fwGames w join fwplayers p on w.row=p.game group by p.game having players > 0
		
		/* ABANDONED METHOD
		// account has heartbeat within 2 minutes?
		$query = "select row from accountHeartbeat where email=? and timestamp > date_sub(now(),  interval 24 hour)";
		$stmt = $link->prepare($query);
		$stmt->bind_param('s',  $email);
		$stmt->execute();
		$stmt->store_result();
		$count = $stmt->num_rows;
		if($count>0){
			echo $count;
		}
		
		// account is currently logged in?
		$query = "select online from accounts where email=?";
		$stmt = $link->prepare($query);
		$stmt->bind_param('s',  $_SESSION['email']);
		$stmt->execute();
		$stmt->store_result();
		$stmt->bind_result($dbonline);
		while($stmt->fetch()){
			$online = $dbonline;
		}
		echo $online;
		*/
		
		
		
	/*
	// get game players
	$query = "select player, account, nation, flag from `fwPlayers` where game=?";
	$stmt = $link->prepare($query);
	$stmt->bind_param('i', $_SESSION['gameId']);
	$stmt->execute();
	$stmt->store_result();
	$stmt->bind_result($dPlayer, $dAccount, $dNation, $dFlag);
	
	$players = array();
	while($stmt->fetch()){
		$x = new stdClass();
		$x->player = $dPlayer;
		$x->account = $dAccount;
		$x->nation = $dNation;
		$x->flag = $dFlag;
		array_push($players, $x);
	}
	*/